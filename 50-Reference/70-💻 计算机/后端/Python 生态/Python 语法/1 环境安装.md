- 安装 pyenv
```
brew install pyenv
```
- 在 ~/.zshrc 末尾添加以下内容
```
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"
```
- 重新加载配置
```
source ~/.zshrc
```
- 安装 Python 3.14
```
pyenv install 3.14
```
- 验证是否生效
```
pyenv --version
```