# Changelog

## 1.6.0 — 2026-08-18

- 新增“自测遮罩模式”：阅读时默认遮住标注文字，点击或按 Enter 后揭晓，适合把页边标注直接变成主动回忆提示。
- 普通阅读模式下也可随时单击标注文字进行遮罩与恢复，不改写 Markdown，不改变标签布局。
- 双击或按 Shift+Enter 继续编辑标注；修复较慢双击被误判为单击、导致编辑弹窗无法打开的问题。
- 自测模式会同步到主窗口和弹出窗口，切换时自动清理旧的逐条遮罩状态。
- 补齐键盘状态、触摸反馈与减少动态效果支持；打印和 PDF 导出始终显示完整标注文字。

## 1.5.1 — 2026-08-12

- 标注中心每条记录新增悬浮操作：编辑标注、开关原文高亮、复制“原文 + 标注”以及删除标注。
- 面板操作会按原标注身份安全定位；文档偏移变化时自动重新匹配，重复内容无法唯一确认时拒绝误改。
- 阅读模式支持双击手写标注文字直接编辑，并支持键盘聚焦后按 Enter 或空格打开。
- 新增“上一条标注”和“下一条标注”命令，默认快捷键为 `⌘/Ctrl + Alt + ↑/↓`，支持首尾循环。
- 阅读滚动时，标注中心自动突出视口附近的标注，并以克制的强调色状态保持面板位置可见。
- 全库面板可直接操作尚未打开的 Markdown 文件；开关高亮和删除使用原子文件更新，不需要先打开文章。

## 1.5.0 — 2026-08-12

- 将原“标注大纲”升级为“标注中心”，可在“当前文档”和“整个仓库”之间快速切换。
- 新增全库标注索引，按文件分组展示目标原文、标注内容、颜色、位置与文件路径。
- 新增原文、标注内容和文件路径的联合搜索，并支持按全部九种语义颜色筛选。
- 点击全库结果会复用已打开的正文页签，或打开对应 Markdown 文件，再定位并居中显示目标原文。
- 全库索引仅在首次打开标注中心或切换到全库模式时构建；之后根据 Markdown 文件的创建、修改、重命名和删除增量更新。
- 编辑器中的未保存修改会立即写入内存索引；异步磁盘读取不会覆盖更新的编辑内容。
- 首次索引采用分批读取，并为不可读文件、快速移动文件和索引加载过程增加安全回退与明确状态。
- 标注中心新增紧凑范围切换、搜索栏、颜色筛选器、文件卡片和匹配数量提示。

## 1.4.16 — 2026-08-09

- 修复快速输入后切换标签页时，旧文档的 200ms 防抖刷新会把标注大纲切回旧内容的问题。
- Crisp 系列授权产品名单补齐 Crisp Organize 与 Crisp Base，并更新开发依赖与版本信息。

## 1.4.15 — 2026-08-08

- 设置页、标注弹窗、快速标注与大纲视图全面汉化；颜色/位置/箭头/字体选项改为中文（颜色主题预设名保持英文）。

## 1.4.14 — 2026-08-07

- 编辑器装饰性能优化：CodeMirror 扩展现在缓存标注解析结果，仅在文档变化时重新解析，光标移动不再触发全文 `findAnnotations`。大量标注文档的输入延迟显著降低。
- 大纲视图刷新防抖：`editor-change` 事件不再每次按键都同步解析全文并重建大纲 DOM，改为 200ms 防抖。切换标签页时仍立即刷新。
- 新增 `buildEditorPreviewRangesFromAnnotations` 导出，支持外部传入预解析的标注数组。

## 1.4.13 — 2026-08-04

- 授权在线校验修复：服务端吊销或设备数超限的拒绝现在会被客户端采信（仅网络异常时降级为离线验签）。

## 1.4.12 — 2026-08-01

- 支持选中多行文字一起标注：跨行高亮与渲染一致，原文换行保留；目标内仍禁止 `==` 标记。

## 1.4.11 — 2026-08-01

- 修复选中文字带首尾空白时提示“must use trimmed text”的问题：自动裁剪空白并保留原文空白。

## 1.4.10 — 2026-07-31

- 授权校验机制更新：此版本仅接受新版授权码，旧版授权码需联系重新签发。

## 1.4.9 — 2026-07-31

- 授权校验升级：启用双公钥过渡机制，存量授权码不受影响。
- 修复 Ed25519 验签 ArrayBuffer byteOffset 边界问题。

## 1.4.6 — 2026-07-29

- 设置页底部新增 `About Crisp Annotations`，用一句话说明插件核心用途。
- 作者统一标注为“小红书 letschips”，并链接到作者主页。
- 新增 About 区块 DOM 与外链安全属性回归测试；完整门禁现为 15 个测试文件、89 项测试。

## 1.4.5 — 2026-07-29

- 修复从右侧 `Annotations outline` 点击标注时，只移动隐藏编辑器光标、阅读页面没有实际跳转的问题。
- 点击大纲项目现在会激活对应正文页签；阅读模式平滑滚动到目标，编辑模式居中定位到原文。
- 到达目标后使用 Obsidian 原生的短暂高亮提示，方便在长文章中立即识别对应标注。

## 1.4.4 — 2026-07-29

- 为右键菜单的 `Add annotation` / `Edit annotation` 项目注册并使用专属 SVG 笔记手账图标。
- 新增 `Orange` 橙色（`#FC9445`）标注颜色选项，弹窗 Color 选项扩展为 9 个，重构为与 Placement 罗盘 1:1 尺寸对齐的 3×3 精密网格阵列。

## 1.4.3 — 2026-07-28

- 八方向罗盘与颜色选择改为原生按钮，补齐键盘操作、读屏标签和 `aria-pressed` 选中状态。
- 罗盘缩放反馈只在支持悬停的精细指针设备启用，触屏不再出现粘滞缩放；选择项过渡改为显式属性。
- 新增弹窗可访问性与动效边界回归测试；完整门禁现为 14 个测试文件、87 项测试。

## 1.4.2 — 2026-07-28

- 页边标注会识别同一阅读窗格中的 Crisp Reading Rail，并为可见轨道预留右侧安全距离。
- 右侧空间不足时，固定右侧布局自动回退为行内标注，智能页边布局改用可用的左侧或行内位置，避免标注与轨道重叠。
- 新增 Reading Rail 共存回归测试；完整门禁现为 14 个测试文件、85 项测试。

## 1.4.1 — 2026-07-28

- 修复禁用或卸载插件后，已打开窗口的 `data-crisp-ann-theme` 主题标记仍残留在 `body` 上的问题。
- 补充卸载清理回归测试；完整门禁现为 14 个测试文件、84 项测试。

## 1.4.0 — 2026-07-27

- 新增 `Quick annotation` 命令，用单行小弹窗快速填写标注；空注释会留在弹窗内提示，不再写入无效语法。
- 新增 `Annotations outline` 侧栏，可浏览当前文档的全部标注并跳回正确原文；编辑内容或切换文档时同步刷新。
- 新增 `Export annotations summary to clipboard` 命令，可把当前文档标注整理为 Markdown 摘要。
- 新增 Classic Crisp、Morandi Muted、Kindle Paper、Cyberpunk Neon 四套全局颜色主题，以及 `Wavy line`、`Double line` 两种连接线。
- 新增“记住上次选择”，可复用最近使用的方向、颜色与高亮开关。
- 方向选择改为直观的八方向罗盘，颜色改为带色点的快速选择。
- 修复阅读模式中重复标签 ID 导致页边标注只显示最后一条、连接线堆叠的问题。
- 修复页边布局销毁或重排后残留旧连接线的问题。
- 修复首次打开标注大纲时可能读取错误文档，以及从大纲点击后可能跳到其他分栏的问题。
- 重新收紧添加/编辑弹窗的尺寸、留白与层级，移除标题和底部操作区的不必要分隔线，并补齐减少动态效果支持。

## 1.3.0 — 2026-07-23

- 将添加/编辑标注弹窗改为紧凑的无分隔线布局，统一留白、圆角、字段层级与按钮样式。
- 方向和颜色改为并列选择，阅读外观改为轻量摘要标签，窄屏自动切换为单列。
- 从弹窗进入外观设置时保留尚未提交的注释草稿。
- 新增弹窗内空值提示以及 `⌘/Ctrl Enter` 快速提交。
- 修复同一段落同时包含上方和下方标注时，后一个标注会错误移除前一个所需间距的问题。
- 修复标注被删除或阅读视图关闭后，旧段落间距、布局定时器和观察器可能残留的问题。

## 1.3.0-beta.2 — 2026-07-23

- 标注弹窗新增当前阅读外观摘要，并可直接进入插件视觉设置。
- 页边布局下把 `Placement` 明确为偏好位置，说明智能布局可能自动均衡两侧。
- 设置页按默认值、阅读布局、标注外观、连接线和编辑模式重新分组。
- 页边宽度、自定义字体和自定义曲率只在对应模式下显示。
- 将原单圈旋转连接线重画为连续多圈、朝箭头方向展开的 `Coiled spiral`。

## 1.3.0-beta.1 — 2026-07-23

- 新增 `Inline`、`Smart margins`、`Left margin` 和 `Right margin` 四种阅读模式布局。
- 新增 `140–260 px` 页边标注宽度设置。
- 智能页边模式会根据原标注方向和两侧数量自动分配左右位置。
- 同侧标注自动错开，连接线随箭头样式、线型和标注颜色变化。
- 窄窗口、分栏空间不足和移动设备自动退回行内布局。
- 保持标注文字位于原始 DOM 内，兼容 Obsidian 阅读模式的增量重渲染。

## 1.2.0 — 2026-07-23

- 新增四种全局箭头样式：手绘、直线、自定义曲线和旋转曲线。
- 新增实线与虚线选项，虚线模式保留清晰的实线箭头尖。
- 新增 `-100` 到 `100` 的曲率控制，可改变弯曲方向和幅度。
- 新增 `Custom` 标注颜色，支持取色器以及三位或六位十六进制色号。
- 自定义颜色同时作用于箭头、连接线、标签和轻量高亮。
- 保持旧版手绘实线为默认视觉，已有标注和设置自动兼容。

## 1.1.0 — 2026-07-23

- 新增标注字体模式：插件手写字体、跟随正文字体和自定义字体。
- 字体切换即时生效，并覆盖主窗口和已打开的弹出窗口。
- 旧版本设置自动迁移，默认视觉保持不变。

## 1.0.0 — 2026-07-23

- 支持通过编辑器右键菜单添加、编辑和移除标注。
- 修复目标中包含单个 `=` 时无法解析的问题。
- 避免把 YAML frontmatter、行内代码和围栏代码块中的示例语法识别为标注。
- 严格校验属性名、重复属性、尾随内容与目标边界，防止半解析状态。
- 修复光标位于标注末尾时仍展开元数据的问题。
- 优化多行高亮的连续性和行高，移除视觉上的多余横线。
- 为中文手写标签补充稳定字体回退。
- 让彩虹色动画更平滑、克制，并遵循“减少动态效果”设置。
- 手机和平板模式采用更安全的下方标签布局。

## 0.1.1 — 2026-07-23

- 修复多行目标在换行处出现不一致底边的问题。

## 0.1.0 — 2026-07-23

- 首个可用版本。
