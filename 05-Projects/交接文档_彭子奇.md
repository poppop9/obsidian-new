# 订单系统
## 采购 & 应付需求
嗯 😐，业务相关的内容貌似没有什么好说的 ……

## 订单系统重构
### 新架构
这部分内容在 `canrd-erp` 的 `docs/ARCHITECTURE_GUIDE.md` 里描述的很详细了，这里我就补充一些题外话

**当前这套架构不是纯血 DDD**
- 纯血的 DDD 是有 application 层的。其作用是用于编排 domain 和 repository 从而实现完整的业务需求，但是由于本套系统要使用工作流来编排业务，所以其实也就替代了 application 层的功能，所以我在设计架构时就把 application 层砍掉了。
- 纯血的 DDD 中 domain 领域之间是不能直接调用的，是要通过领域事件进行通信的，但目前我设计的 domain 之间是直接调用的。因为根据我所学的知识来看，DDD 这么处理是为了微服务做准备，为了将来划分微服务更加便利，我认为我们公司目前的团队规模不太可能把订单系统拆成微服务，而且加上领域事件要引入 redis 或者 kafka 等中间件，增加了系统复杂度，我主观认为没有必要这么干。

### 工作流
这部分内容在 `canrd-erp` 的 `docs/WORKFLOW_GUIDE.md` 里描述的很详细了，这里我就补充一些题外话
- 推荐安装 idea 插件 [LiteFlow-Helper](https://plugins.jetbrains.com/plugin/index?xmlId=com.yomahub.LiteFlow-Helper&utm_source=product&utm_medium=link&utm_campaign=IU&utm_content=2026.1)，可以在右侧窗口快速跳转到对应的节点，还能从对应节点快速跳转到对应的组件
- 我个人认为，如果订单的流程直到目前为止依旧变动频繁，那其实并不适合将其用 xml 重构成工作流。因为如果订单的流程变动频繁，其实每一次的改动成本会比重构之前大很多，不仅要改动 xml 文件，还要考虑改动前后的适配问题

### 数据库
其实我有在想，数据库的某些表结构是否有些不太合理了、某些字段是否也是冗余的无用的

# 科言猫
## 书籍下载
整个书籍下载的脚本文件，以及下载好的书籍源文件都放在 243 服务器的 `/mnt/DATA/book-files` 下。以下是目录结构说明：

```
/mnt
└── DATA
    └── book-files
        ├── ia_downloads/       # IA 书籍下载目录（源文件）
        ├── ia_resume/          # IA 下载缓存目录（用于断点续传）
        ├── ia_torrents/        # IA 种子文件目录
        ├── libgen_downloads/   # LibGen 书籍下载目录（源文件）
        ├── libgen_resume/      # LibGen 下载缓存目录（用于断点续传）
        ├── libgen_torrents/    # LibGen 种子文件目录
```
### libgen
libgen 的元数据存储在 242 服务器 mysql 的 `libgen` 数据库里

4053 / 4056 整体下载完了，有三个 torrent 种子在 annas-archive 处于丢失状态，所以导致了下载的失败。统计了一下大概有 46 + 46 + 25 本书。

解决办法是这三条数据在 mysql 里都有 `md5_list` 字段，拿着这些 `md5` 去请求 annas-archive 的 api 进行下载即可

![](https://order-erp.oss-cn-qingdao.aliyuncs.com/1783499390537-2646-image.png?Expires=2099118590&OSSAccessKeyId=LTAI5t8KJVEXp3k5CT8pt9wG&Signature=rhrT5q6Nj88%2BRZsTT5iuG6l7wKU%3D)
![](https://order-erp.oss-cn-qingdao.aliyuncs.com/1783499482437-7445-image.png?Expires=2099118684&OSSAccessKeyId=LTAI5t8KJVEXp3k5CT8pt9wG&Signature=cvMcZkFsy6kD3tyOrQ2rsDiQ5Vk%3D)
![](https://order-erp.oss-cn-qingdao.aliyuncs.com/1783499652252-1459-image.png?Expires=2099118854&OSSAccessKeyId=LTAI5t8KJVEXp3k5CT8pt9wG&Signature=zvGIFJf9ApjaWIwccwx262iRfoI%3D)

### ia
ia 的元数据存储在 242 服务器 mysql 的 `ia_books_metadata` 数据库里

ia 种子质量不好，整体下载速度偏慢

### zlib3
zlib3 的元数据存储在 242 服务器 mysql 的 `zlib_metadata` 数据库里

之前将 json 数据格式化到 mysql 任务受异常情况影响中断了，只导入了 7000w+ 条数据，后续要继续导入

后续筛选书籍可以使用 `title`、`description` 字段










